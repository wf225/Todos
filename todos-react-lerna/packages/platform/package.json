{
  "name": "todos-platform",
  "version": "0.1.1",
  "description": "Todos-platform",
  "main": "./lib/index.js",
  "author": "Bill Wu",
  "license": "ISC",
  "dependencies": {
    "react": "15.4.2",
    "react-dom": "15.4.2",
    "react-slot-fill": "1.0.0-alpha.10"
  },
  "devDependencies": {
  },
  "scripts": {
    "dll": "webpack --config webpack.dll.config.js",
    "build": "npm run compile",
    "compile": "babel --presets es2015,react -d lib/ src/",
    "prepublish": "npm run compile",
    "test": "jest -u --coverage"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "jest": {
    "moduleFileExtensions": [
      "js",
      "jsx"
    ]
  }
}
